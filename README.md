# Galgame 报错解决百科网站

一个专门收集和分享 Galgame 报错解决方案的百科网站，使用原生 PHP + MySQL 开发。

## 功能特性

### 前台功能
- **首页**：展示搜索框、最新游戏、常见报错分类
- **全站搜索**：支持搜索游戏名、报错内容、VNDB 编号
- **游戏详情页**：展示游戏信息 + 该游戏所有已审核报错
- **用户提交报错**：可填写游戏ID、报错分类、标题、现象、系统、汉化补丁、解决方案、上传截图
- **审核机制**：所有用户提交的报错必须经过管理员审核才能显示

### 后台功能
- **独立后台登录**：安全的密码验证机制
- **控制台**：统计数据展示、最新待审核报错、快捷操作
- **游戏管理**：查看所有游戏、添加游戏、删除游戏、通过 VNDB 编号自动抓取游戏信息
- **报错分类管理**：可自由添加、编辑、删除报错类型
- **报错管理**：查看所有用户提交的报错、审核通过/删除、查看报错截图

### 核心特色功能
- **VNDB 集成**：用户提交报错时，如果网站没有该游戏，可通过 VNDB ID 自动创建游戏
- **一键抓取**：后台可一键抓取 VNDB 游戏信息自动填充
- **多图上传**：支持上传多张报错截图
- **响应式设计**：支持手机/电脑访问
- **安全可靠**：代码干净、注释清晰、可直接上线运行

## 技术栈

- **后端**：原生 PHP 7.4+
- **数据库**：MySQL 5.7+ / MariaDB 10.2+
- **前端**：HTML5 + CSS3 + JavaScript
- **样式**：深浅色主题 + 玻璃拟态卡片 + 响应式布局

## 界面风格

- 深浅色双主题，支持一键切换
- 玻璃拟态卡片与渐变强调色
- 顶部导航前后台统一（PC + 移动端）
- 响应式布局，兼容桌面与移动设备
- 信息密度与可读性平衡，适合内容检索与管理操作

## 目录结构

```
www.galerror.top/
├── admin/                  # 后台管理目录
├── api/                    # 前后台接口
├── assets/                 # 静态资源（CSS/JS）
│   ├── css/
│   └── js/
├── data/                   # 上传与运行数据（已在 .gitignore 过滤）
├── includes/               # 公共文件（配置、鉴权、模板片段）
├── index.php               # 首页
├── messages.php            # 站内信
├── private_messages.php    # 私信通知
├── page.php                # 站点页面（含管理员须知）
├── prepush_check.bat       # 本地推送前安全检查脚本
└── README.md               # 项目说明文档
```

## 安装部署

### 1. 环境要求

- PHP 7.4 或更高版本
- MySQL 5.7+ 或 MariaDB 10.2+
- Web 服务器（Apache/Nginx）
- PHP 扩展：PDO、PDO_MySQL、GD、JSON、cURL

### 2. 数据库配置

1. 创建数据库和用户：
```sql
CREATE DATABASE galgame_errors CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'galgame'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON galgame_errors.* TO 'galgame'@'localhost';
FLUSH PRIVILEGES;
```

2. 导入数据库结构：
```bash
mysql -u galgame -p galgame_errors < database.sql
```

### 3. 配置文件

修改 `includes/config.php` 中的数据库配置：

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'galgame_errors');
define('DB_USER', 'galgame');
define('DB_PASS', 'your_password');
```

### 4. 目录权限

确保以下目录可写：
```bash
chmod 755 data/
```

### 5. Web 服务器配置

#### Apache 配置示例
```apache
<VirtualHost *:80>
    ServerName your-domain.com
    DocumentRoot /path/to/www.galerror.com
    
    <Directory /path/to/www.galerror.com>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

#### Nginx 配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/www.galerror.com;
    index index.php index.html;
    
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

## 默认账户

- 首次部署请在数据库中手动创建管理员账户，或通过现有初始化流程创建。
- 不建议保留固定默认密码。

⚠️ **安全提醒**：上线前请确认管理员密码强度、邮箱验证与密钥配置。

## 使用说明

### 用户使用流程

1. **搜索报错**：在首页或搜索页面输入游戏名、报错内容或 VNDB 编号
2. **浏览游戏**：点击游戏查看该游戏的所有报错解决方案
3. **提交报错**：如果遇到新的报错，可以通过提交页面分享解决方案
4. **VNDB 集成**：提交时输入 VNDB ID 可自动获取游戏信息

### 管理员使用流程

1. **登录后台**：访问 `/admin/` 目录，使用管理员账户登录
2. **审核报错**：在报错管理页面审核用户提交的内容
3. **管理游戏**：通过 VNDB ID 添加游戏，或手动创建游戏条目
4. **分类管理**：自定义报错分类，方便用户分类查找

## API 接口

### VNDB API

系统集成了 VNDB API，支持通过 VNDB ID（如 v12345）自动获取游戏信息：

- 游戏标题
- 日文名
- 开发商
- 发售日
- 封面图
- 平台

## 安全特性

- **SQL 注入防护**：使用 PDO 预处理语句
- **XSS 防护**：所有输出都经过 htmlspecialchars 处理
- **CSRF 防护**：重要操作需要确认
- **文件上传安全**：限制文件类型和大小
- **会话安全**：安全的登录验证机制

## 性能优化

- **数据库索引**：关键字段建立索引
- **分页查询**：避免一次性加载大量数据
- **图片优化**：支持外部图片链接
- **缓存机制**：可扩展 Redis 缓存

## 扩展功能

系统设计支持以下扩展：

1. **用户系统**：可添加用户注册、登录功能
2. **评论系统**：可为解决方案添加评论
3. **点赞功能**：用户可为有用的解决方案点赞
4. **标签系统**：为游戏和报错添加标签
5. **API 接口**：提供 RESTful API 供第三方调用

## 故障排除

### 常见问题

1. **数据库连接失败**
   - 检查数据库配置信息
   - 确认数据库服务正在运行
   - 检查用户权限

2. **文件上传失败**
   - 检查 uploads 目录权限
   - 确认 PHP 上传配置
   - 检查磁盘空间

3. **VNDB API 调用失败**
   - 检查网络连接
   - 确认 cURL 扩展已启用
   - 检查 VNDB 服务状态

### 日志查看

- PHP 错误日志：`/var/log/php_errors.log`
- Web 服务器日志：根据服务器配置查看

## 更新说明

> 版本号采用语义化格式（主.次.修订）。次版本对应一批功能/体验迭代，主版本对应架构级变更。
> 以下记录依据后台待办（`todos` 表）已完成项按时间归并整理，仅收录已上线内容。

### v2.1.0 (2026-06-20) — 重构后功能扩展与体验修复
- 修复30天登录失效问题
- 新增点赞功能：游戏 / 文章 / 报错 / 解决方案 / 讨论均可点赞，赞数计入内容提交者；被赞向提交者发送站内通知并支持点击跳转
- 个人主页数据统计、用户悬浮卡片新增「被赞数」，首页预览卡片显示被赞数图标与数量
- 基于个人主页数据统计的 9 项指标新增站内排行榜（顶部导航栏入口）
- 个人主页新增「解决方案」标签页；游戏/报错/解决方案/文章/话题列表的查看功能集成到标题，取消查看与编辑按钮（不再从个人主页编辑）
- 修复私信列表内容预览显示 JSON 原文的问题
- 修复移动端文章导航目录消失、正文图片懒加载导致的目录跳转定位失效
- 修复后台报错/游戏/解决方案管理无法翻页、解决方案截图无法查看
- 修改记录相关修复：系统分类显示中文标签、无改动记录不再显示、封面变更与报错分类变更纳入差异对比

### v2.0.0 (2026-06-20) — 架构重构（参考《docs/1.整改任务清单-v2.md》）
- 完全 Twig 化：页面渲染、业务逻辑、交互脚本拆分为入口 / service·query·actions / Twig partial / 独立 JS，统一后台布局与组件复用
- 安全加固：密钥从源码迁出，nginx 封禁 `/includes/`、`data/*.php` 等敏感路径，上传接口三层校验
- `config.php` 剥离 43 个迁移函数（1737→974 行），新增 schema 版本守卫与 CLI 迁移工具，迁移不再随页面触发；下线 6 个后台裸奔迁移入口
- 评论三套脚本合并为 `comment-core.js` 共享核心，修复重复提交与双重确认弹框
- 死代码清理：删除 7 个无引用私信文件、约 250 行无用 JS，处理跨文件 CSS 冲突
- 新增统一日志器、健康检查页、模板变量契约注释；完成 `data/` 审计与备份脚本、部署回滚手册与问题台账

### v1.14.0 (2026-05-23) — 方案体系重构与 Twig 引入
- 区分报错提交者与方案提交者，新增 `error_solutions` 独立表，修复两套方案数据混乱
- 报错/解决方案详情页：截图嵌入正文、单报错多方案、编辑回填、新增模式
- 数据统计新增「修正方案数」
- 增量引入 Twig 模板引擎（最小化接入，保留原 SQL / 分页 / 筛选）
- 新增按系统/设备分类搜索；移动端表格显示优化

### v1.13.0 (2026-05-09) — 顶部栏统一与安全加固
- 统一 PC/移动端前后台顶部导航栏，后台复用前台头部组件，移除后台独立顶栏与标题区
- 统一 CSRF 防护、API 鉴权与限频（rate limit / basic auth）、参数白名单校验
- VNDB 抓取改为直连 + 中转双通道自动切换，新增缓存/熔断/降级与后台状态开关
- 报错提交新增系统分类（7 类）与安卓模拟器设备信息项；新增报错/解决方案列表页
- 修复浏览量不计数、CSRF 校验失败、后台层级与菜单等问题

### v1.12.0 (2026-05-05) — 移动端体系与稳定性
- 移动端全面优化：后台列表/导航/统计卡片、私信键盘跟随与发送按钮、表格显示
- 修复频繁自动登出，人机验证仅在登录/注册触发
- SEO 收录修复（robots / sitemap / SEO 标签）
- 评论修复：空格吞并、网络误报、实时刷新；私信滚动与输入框优化；草稿提交后清理
- 敏感词日志改为记录命中用户名与场景

### v1.11.0 (2026-04-29) — 编辑器与封面体验
- Markdown 表格增强（自定义列数修复）与「文本转表格」工具
- Markdown 标签交互、草稿（标题/标签/正文）、有序列表自动编号
- 封面裁剪重构（Cropper.js，横竖屏自适应）、游戏详情页封面自适应与点击预览
- 后台审核显示提交者名称替代 IP

### v1.10.0 (2026-04-24) — 评论体系升级与交互完善
- 评论区全面升级：文章评论、回复精确引用展示、@提及联想、站内信跳转定位、分页/折叠/页码跳转
- 新增用户头像悬停信息卡片；前台待办展示组件与完成时间字段
- 后台按钮统一美化；管理员发布内容免审
- 大量修复：链接识别边界、图片上传与渲染、图片/文字链接 404、站内信跳转与已读、讨论区统计等

### v1.9.0 (2026-04-18) — 站点页面与安全治理
- URL 美化规则上线
- 关于我们 / 版权及法律声明 / 入站须知 / 管理员须知页面与后台统一管理，重写法务文案（非盈利、开源）
- 用户管理与管理员设置信息同步并新增角色信息；管理员拒绝审核需填理由并站内信通知
- CDN/隐藏真实 IP 相关加固；精简冗余代码；页脚版权信息；话题数量统计修复

### v1.8.0 (2026-04-15) — 邮件、域名与主题
- 邮件链路优化：Resend API 直发、重配 SMTP、修复验证邮件过慢
- localhost 链接改为域名；日间/夜间独立背景图

### v1.7.0 (2026-04-08) — 私信与内容安全
- 站内私信功能（对话列表式）；私信/评论敏感词过滤（Sensitive-lexicon 词库）

### v1.6.0 (2026-04-06) — 社区互动
- 首页新增讨论区，登录用户可发布话题；讨论区与报错解决方案区开放评论

### v1.5.0 (2026-04-03) — 编辑器与后台
- 富文本编辑器改造为 Markdown 编辑器
- 后台布局调整与侧边栏图标
- 人机验证（滑块）与邮箱验证改用 Resend API

### v1.4.0 (2026-04-02) — 浏览量与详情页
- 浏览量可视化统计（登录/访客去重）
- 新增解决方案详情页与游戏详情页解决方案简略卡片

### v1.3.0 (2026-03-31) — 文章系统与搜索
- 提交文章功能 + 审核流程 + 首页「相关文章」栏目
- 搜索框分类（游戏 / 报错内容 / 文章）
- 首页卡片化交互与文章详情页交互优化

### v1.2.0 (2026-03-27) — 用户体系与修改记录
- 前台头像下拉菜单与个人主页（数据统计、内容管理）
- 提交报错/解决方案修改记录对比（新增标绿、删除标红，永久保存）
- 截图字段拆分为「报错截图」「解决方案截图」

### v1.1.0 (2026-03-24) — 体验重构与提交优化
- 前后台样式与布局重构（固定导航栏 / 侧边栏，仅内容区滚动）
- 提交报错支持「是否有解决方案」单选，开放他人为报错补充方案
- VNDB 增加别名抓取，游戏标题优先级（中文 → 日文 → 英文/罗马音）
- 去除后台控制台系统信息

### v1.0.0 (2026-03-22) — 初始版本
- 初始版本发布
- 完整的前后台功能
- VNDB 集成
- 响应式设计

## 技术支持

如遇到问题，请检查：

1. 服务器环境是否符合要求
2. 数据库配置是否正确
3. 目录权限是否设置正确
4. PHP 错误日志中的具体错误信息

## 开源协议

本项目采用 MIT 协议开源，可自由使用和修改。

---

**注意**：本网站仅用于技术交流和分享，请遵守相关法律法规，尊重知识产权。
