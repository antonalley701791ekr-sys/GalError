<div class="card">
    <div class="card-header">编辑管理员：{{ editAdmin.username }}</div>
    <div class="card-body">
        <form method="post">
        {{ csrf_input('admin_form')|raw }}
            {% include 'admin/partials/users_form_fields.twig' with { mode: 'edit', editAdmin: editAdmin, permModules: permModules, actionLabels: actionLabels } %}
            <div class="btn-group">
                <button type="submit" class="btn">保存修改</button>
                <a href="users.php" class="btn btn-secondary">取消</a>
            </div>
        </form>
    </div>
</div>
