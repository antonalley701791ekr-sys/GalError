<div class="admin-page-header">
    <h1>用户管理</h1>
    <div style="display:flex;align-items:center;gap:8px;color:var(--text-secondary);font-size:0.9rem;">
        <span>在线用户</span>
        <span id="onlineUsersCount" style="color: var(--accent-green, #34d399); font-weight: 700;">{{ onlineUsersCount }}</span>
    </div>
</div>
