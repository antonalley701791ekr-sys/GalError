<div class="card">
    <div class="card-header">添加子管理员</div>
    <div class="card-body">
        <form method="post">
        {{ csrf_input('admin_form')|raw }}
            {% include 'admin/partials/users_form_fields.twig' with { mode: 'add', permModules: permModules, actionLabels: actionLabels } %}
            <div class="btn-group">
                <button type="submit" class="btn">创建管理员</button>
                <a href="users.php" class="btn btn-secondary">取消</a>
            </div>
        </form>
    </div>
</div>
