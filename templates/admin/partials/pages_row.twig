<tr><td><strong>{{ (pageMeta[p.slug]|default({'title': p.title})).title }}</strong></td><td class="text-muted">{{ (pageMeta[p.slug]|default({'desc': ''})).desc }}</td><td><a href="/page/{{ p.slug }}" target="_blank" style="font-size:0.85rem;">/page/{{ p.slug }}</a></td><td style="font-size:0.85rem;">{{ p.updated_at ? p.updated_at|date('Y-m-d H:i') : '-' }}</td><td><a href="?action=edit&slug={{ p.slug }}" class="btn btn-sm">编辑</a></td></tr>
