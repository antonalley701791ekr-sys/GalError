<div class="card settings-section"><div class="card-header">VNDB 抓取优化</div><div class="card-body"><div class="form-group"><label class="toggle-label"><input type="checkbox" name="vndb_opt_enabled" value="1" class="toggle-checkbox" {{ settings.vndb_opt_enabled|default('1') == '1' ? 'checked' : '' }}><span class="toggle-switch"></span><span>启用抓取优化</span></label><p class="form-hint">先试一个更快的请求方式；如果不稳定，会自动退回你现在一直在用的旧方法。</p></div><div class="form-group"><label class="form-label">连续失败阈值</label><input type="number" name="vndb_opt_fail_threshold" class="form-input" min="1" value="{{ settings.vndb_opt_fail_threshold|default('5') }}" style="max-width: 240px;"><p class="form-hint">优化方法连续失败多少次后，暂时停用优化方法，直接用旧方法。</p></div><div class="form-group"><label class="form-label">停用优化时长（秒）</label><input type="number" name="vndb_opt_open_seconds" class="form-input" min="30" value="{{ settings.vndb_opt_open_seconds|default('600') }}" style="max-width: 240px;"><p class="form-hint">达到失败阈值后，优化方法会暂停这段时间；时间到了会再自动尝试。</p></div><div class="form-group"><label class="form-label">VNDB 缓存时长（秒）</label><input type="number" name="vndb_cache_ttl" class="form-input" min="60" value="{{ settings.vndb_cache_ttl|default('21600') }}" style="max-width: 240px;"><p class="form-hint">缓存越长，请求越少、速度越快；但数据更新会稍慢。默认 21600 秒（6 小时）。</p></div></div></div>
